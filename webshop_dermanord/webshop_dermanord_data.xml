<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data noupdate="1">
        <!-- Blog Experiences -->
        <record id="blog_experiences" model="blog.blog">
            <field name="name">Experiences</field>
            <field name="subtitle"></field>
        </record>

        <!-- image 4:3 -->
        <record id="image_four_three_small" model="image.recipe">
            <field name="name">Image Normal small (4:3)</field>
            <field name="description">For normal images, aspect ratio 4:3</field>
            <field name="recipe">if image.width >= image.height: #landscape
    if float(image.width)/image.height >= float(4)/3:
        image.transform(resize='%sx>' %int(float(image.width)/image.height*200))
    else:
        image.transform(resize='%sx>' %int(float(200)*4/3*image.height/image.width))
    image.crop(width=int(float(200)*4/3), height=200, gravity='center')
else: #portrait
    image.transform(resize='200x>')
    image.crop(width=int(float(200)/image.height*image.width), height=200, gravity='center')
            </field>
        </record>
    </data>
</openerp>
